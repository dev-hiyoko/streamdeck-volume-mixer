{
  "name": "streamdeck-volume-mixer",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "packageManager": "npm@11.13.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "esbuild src/plugin.ts --bundle --platform=node --target=node20 --format=cjs --outfile=fun.hiyoko.volumemixer.sdPlugin/bin/plugin.js",
    "watch": "npm run build -- --watch",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@elgato/streamdeck": "^2.0.0",
    "@elgato/utils": "^0.4.5",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  }
}
